--- /tmp/claude-1000/-home-teo-TesisExactas/8627b924-8715-47f1-beb3-a367a5241b0b/scratchpad/extract/Speculative_Rag_Organizado_Backup_local_version_nofinetune_v2_hotpot_solver/Speculative_Rag_Organizado_Backup_local_version_nofinetune_v2_hotpot_solver/TraditionalRag_CombinedRetrieve/Benchmark/benchmark_traditional_combined_question.py	2025-05-19 12:05:00.000000000 -0300
+++ /tmp/claude-1000/-home-teo-TesisExactas/8627b924-8715-47f1-beb3-a367a5241b0b/scratchpad/extract/Speculative_Rag_Organizado_Backup_local_version_nofinetune_v2_triviaQA_solver/Speculative_Rag_Organizado_Backup_local_version_nofinetune_v2_triviaQA_solver/TraditionalRag_CombinedRetrieve/Benchmark/benchmark_traditional_combined_question.py	2025-05-09 23:33:06.000000000 -0300
@@ -13,7 +13,7 @@
 from sentence_transformers import SentenceTransformer, util
 from bert_score import score
 from tqdm import tqdm
-from typing import List, Dict, Any, Tuple, Optional
+from typing import List, Dict, Any, Tuple
 from qdrant_client import AsyncQdrantClient
 import torch
 
@@ -37,7 +37,7 @@
 EMBEDDING_MODEL = "nomic-embed-text"
 LLM_MODEL = "hugging-quants/Meta-Llama-3.1-8B-Instruct-AWQ-INT4"
 # Modelo para el cálculo de similitud coseno
-sentence_model = SentenceTransformer('all-MiniLM-L6-v2', device="cuda:1")
+sentence_model = SentenceTransformer('all-MiniLM-L6-v2', device='cuda:1')
 
 
 
@@ -45,7 +45,7 @@
     """
     Calcula la similitud coseno entre dos textos
     """
-    # Convertir a string si es una lista
+    # Convertir a string si es una lista (respuestas exactas pueden ser listas)
     if isinstance(text1, list):
         text1 = str(text1)
     if isinstance(text2, list):
@@ -56,19 +56,8 @@
         return 0.0
     
     try:
-        # Especificar el dispositivo CUDA:1
-        device = torch.device("cuda:1")
-        
-        # Asegurarnos de que el modelo esté en CUDA:1
-        if not hasattr(calculate_cosine_similarity, 'sentence_model'):
-            calculate_cosine_similarity.sentence_model = SentenceTransformer('all-MiniLM-L6-v2', device="cuda:1")
-        
-        # Generar embeddings y moverlos a CUDA:1
-        embedding1 = calculate_cosine_similarity.sentence_model.encode(text1, convert_to_tensor=True)
-        embedding2 = calculate_cosine_similarity.sentence_model.encode(text2, convert_to_tensor=True)
-        
-        embedding1 = embedding1.to(device)
-        embedding2 = embedding2.to(device)
+        embedding1 = sentence_model.encode(text1, convert_to_tensor=True)
+        embedding2 = sentence_model.encode(text2, convert_to_tensor=True)
         
         # Asegúrate de que los tensores sean unidimensionales (vectores)
         if len(embedding1.shape) > 1 and embedding1.shape[0] > 1:
@@ -82,8 +71,7 @@
         if isinstance(similarity, torch.Tensor) and similarity.numel() > 1:
             similarity = similarity.mean()
             
-        # Mover el resultado a CPU antes de convertirlo a Python
-        return similarity.cpu().item()
+        return similarity.item()
     except Exception as e:
         logger.error(f"Error calculando similitud coseno: {e}")
         return 0.0
@@ -155,7 +143,8 @@
 async def generate_traditional_rag_response(
     question: str,
     llm_client: AsyncOpenAI,
-    embeddings_client: AsyncOpenAI
+    embeddings_client: AsyncOpenAI,
+    top_k: int
 ) -> Tuple[str, float, float]:
     """
     Genera una respuesta con Traditional RAG y captura los tiempos
@@ -164,6 +153,7 @@
         question: La pregunta a responder
         llm_client: Cliente para el LLM
         embeddings_client: Cliente para los embeddings
+        top_k: Número de documentos a recuperar
         
     Returns:
         Tuple con (respuesta, tiempo_total, tiempo_recuperacion)
@@ -195,7 +185,7 @@
             "llm_client": llm_client,                # Cliente vLLM para LLM
             "embedding_model": EMBEDDING_MODEL,      # nomic-embed-text (Ollama)
             "llm_model": LLM_MODEL,                  # Llama 3.1 Instruct (vLLM)
-            "top_k": 20,
+            "top_k": top_k,
             "debug_prints": False,
             "verbose_timing": False,
             "question_collection": "questions-index-finetuned"
@@ -290,20 +280,15 @@
     
     return result
 
-async def run_benchmark(
-    limit: int = 300,
-    questions: Optional[List[Dict[str, Any]]] = None,
-    results_dir: Optional[Path] = None,
-    top_k: int = 20
-) -> None:
+async def run_benchmark(questions=None, results_dir=None, top_k=20, n_questions=None):
     """
     Ejecuta el benchmark completo
     
     Args:
-        limit: Número máximo de preguntas a evaluar
         questions: Lista opcional de preguntas predefinidas
-        results_dir: Directorio opcional para resultados
+        results_dir: Directorio opcional para guardar resultados
         top_k: Número de documentos a recuperar
+        n_questions: Número específico de preguntas a procesar
     """
     # Configurar clientes de API
     llm_client = AsyncOpenAI(
@@ -316,22 +301,40 @@
         api_key="ollama"
     )
     
-    # Usar directorio de resultados personalizado si se proporciona
+    # Usar el directorio de resultados proporcionado o el predeterminado
     if results_dir is None:
         results_dir = Path(__file__).parent / "results"
+    else:
+        results_dir = Path(results_dir)
+    
     results_dir.mkdir(exist_ok=True)
     
-    # Crear directorios para resultados
     ideal_dir = results_dir / "ideal_answers"
     exact_dir = results_dir / "exact_answers"
     ideal_dir.mkdir(exist_ok=True)
     exact_dir.mkdir(exist_ok=True)
     
-    # Usar preguntas proporcionadas o obtenerlas de Qdrant
-    if questions is not None:
-        all_questions = questions[:limit]
+    # Manejar las preguntas según los parámetros proporcionados
+    if questions and n_questions:
+        # Si se proporcionaron preguntas y n_questions, usar solo las primeras n_questions
+        questions_to_process = questions[:n_questions]
+        logger.info(f"Usando las primeras {n_questions} preguntas del conjunto proporcionado")
+    elif questions:
+        # Si solo se proporcionaron preguntas, usar todas
+        questions_to_process = questions
+        logger.info(f"Usando todas las preguntas proporcionadas ({len(questions)})")
     else:
-        all_questions = await get_questions_from_qdrant(limit=limit)
+        # Si no se proporcionaron preguntas, obtener de Qdrant
+        limit = n_questions if n_questions else 400
+        questions_to_process = await get_questions_from_qdrant(limit=limit)
+        if n_questions:
+            questions_to_process = questions_to_process[:n_questions]
+        logger.info(f"Obtenidas {len(questions_to_process)} preguntas de Qdrant")
+    
+    # Verificar que tenemos preguntas para procesar
+    if not questions_to_process:
+        logger.error("No hay preguntas para procesar")
+        return
     
     # Verificar checkpoints existentes
     ideal_results = []
@@ -357,40 +360,21 @@
             logger.error(f"Error cargando resultados previos para respuestas exactas: {e}")
     
     # Filtrar preguntas ya procesadas
-    questions_to_process = [q for q in all_questions if q["question_id"] not in processed_questions]
-    
-    total_questions = len(all_questions)
-    remaining_questions = len(questions_to_process)
+    questions_to_process = [q for q in questions_to_process if q["question_id"] not in processed_questions]
     
-    # Analizar distribución de tipos de preguntas
-    question_types = {}
-    for q in all_questions:
-        q_type = "unknown"
-        if "question_id" in q:
-            # Convert question_id to string before checking
-            question_id_str = str(q["question_id"])
-            if "_" in question_id_str:
-                # Intentar inferir tipo a partir del ID si está disponible (e.g., "list_123")
-                id_parts = question_id_str.split("_")
-                if len(id_parts) > 1:
-                    q_type = id_parts[0]
-        question_types[q_type] = question_types.get(q_type, 0) + 1
-    
-    logger.info(f"Total de preguntas: {total_questions}")
-    logger.info(f"Distribución de tipos (inferidos del ID): {question_types}")
-    logger.info(f"Preguntas ya procesadas: {total_questions - remaining_questions}")
-    logger.info(f"Preguntas por procesar: {remaining_questions}")
+    total_questions = len(questions_to_process)
+    logger.info(f"Procesando {total_questions} preguntas nuevas")
     
     # Procesar cada pregunta
     for i, question_data in enumerate(tqdm(questions_to_process, desc="Procesando preguntas")):
         question = question_data["question"]
         question_id = question_data.get("question_id", f"unknown_{i}")
         
-        logger.info(f"Procesando pregunta {i+1}/{len(questions_to_process)}: {question}")
+        logger.info(f"Procesando pregunta {i+1}/{total_questions}: {question}")
         
         # Generar respuesta con RAG tradicional
         response, total_time, retrieval_time = await generate_traditional_rag_response(
-            question, llm_client, embeddings_client
+            question, llm_client, embeddings_client, top_k
         )
         
         # Evaluar respuesta con respuesta ideal
@@ -398,7 +382,8 @@
             ideal_result = await evaluate_question(
                 question_data, llm_client, response, total_time, retrieval_time, "ideal_answer"
             )
-            ideal_results.append(ideal_result)
+            if ideal_result:
+                ideal_results.append(ideal_result)
             
             # Guardar resultados parciales después de cada 5 preguntas
             if len(ideal_results) % 5 == 0:
@@ -410,7 +395,8 @@
             exact_result = await evaluate_question(
                 question_data, llm_client, response, total_time, retrieval_time, "exact_answer"
             )
-            exact_results.append(exact_result)
+            if exact_result:
+                exact_results.append(exact_result)
             
             # Guardar resultados parciales después de cada 5 preguntas
             if len(exact_results) % 5 == 0:
